{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:api": "swagger-cli validate api/openapi.yaml || echo 'swagger-cli not installed'",
    "gen:client": "echo 'Add openapi-typescript or openapi-generator to generate client'"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitejs/plugin-vue": "^4.1.0",
    "vite": "^5.0.0"
  }
}
